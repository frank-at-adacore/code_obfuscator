with "libadalang";

project Default is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("obfuscator.adb");

   package Compiler is
      for Switches ("ada") use ("-g");
   end Compiler;

end Default;

