with "libadalang";

project Default is

   for Source_Dirs use ("src", "src/debug");
   for Object_Dir use "obj";
   for Exec_Dir use "exec";
   for Main use ("obfuscator.adb");

   package Compiler is
      for Switches ("ada") use ("-g", "-gnat2022", "-gnatwa");
   end Compiler;

end Default;

